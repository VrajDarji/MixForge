// Reserved for small, pure helper functions that are neither domain types
// (./types.ts) nor the two core data-shaping functions (./lib.ts). Empty
// for now — no such helpers exist yet in src/core/.

export {};
